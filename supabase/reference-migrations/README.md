# Historical migrations

These files preserve the original schema-design handoff for audit context. They were not
applied to the new Roam project because they assumed an older prototype schema and did not
form a clean, reproducible install.

Do not run these files. The authoritative, applied migration chain is the timestamped set in
`../migrations/`.
